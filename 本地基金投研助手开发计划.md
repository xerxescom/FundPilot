# 本地基金投研助手开发计划（PostgreSQL版）

> 项目定位：一个可以长期自用、可本地部署、可展示在作品集里的「基金监控 + 策略评分 + AI解释」系统。  
> 核心原则：数据和规则本地可控，AI只负责解释和总结，不直接替用户做买卖决策。

---

## 0. 项目目标

### 0.1 最终效果

系统每天自动更新基金数据，并生成：

- 我的基金持仓监控
- 自选基金净值变化
- 候选基金评分排行
- 回撤、波动、排名变化预警
- 基金之间的相关性和重复度分析
- AI每日基金简报
- AI解释某只基金为什么值得关注或不值得关注

### 0.2 技术栈

| 模块 | 技术 |
|---|---|
| 后端接口 | FastAPI |
| 数据库 | PostgreSQL |
| ORM | SQLAlchemy 2.x |
| 数据采集 | AKShare / 东方财富接口 |
| 数据处理 | Pandas / NumPy |
| 定时任务 | APScheduler |
| 可视化看板 | Streamlit / 后期可换 Vue |
| 图表 | Plotly |
| AI总结 | 本地 Ollama / 在线 API |
| 部署 | Docker Compose |

---

## 1. 项目阶段总览

```text
第1阶段：项目骨架和数据库
第2阶段：基金基础数据采集
第3阶段：净值数据入库和更新
第4阶段：指标计算
第5阶段：基金评分模型
第6阶段：FastAPI接口
第7阶段：Streamlit看板
第8阶段：预警系统
第9阶段：AI分析助手
第10阶段：Docker本地部署
第11阶段：测试、日志、文档完善
```

---

# 第1阶段：项目初始化与基础架构

## 1.1 创建项目目录

```text
fund-watcher/
├── app/
│   ├── main.py
│   ├── core/
│   │   ├── config.py
│   │   └── logging.py
│   ├── db/
│   │   ├── session.py
│   │   ├── base.py
│   │   └── models/
│   ├── schemas/
│   ├── api/
│   │   └── v1/
│   ├── services/
│   ├── data_source/
│   ├── jobs/
│   └── utils/
├── dashboard/
│   └── streamlit_app.py
├── scripts/
├── tests/
├── docker-compose.yml
├── Dockerfile
├── pyproject.toml
├── .env.example
└── README.md
```

## 1.2 初始化 Python 环境

建议使用 `uv`：

```bash
uv init fund-watcher
cd fund-watcher
uv add fastapi uvicorn sqlalchemy psycopg2-binary asyncpg pydantic pydantic-settings
uv add pandas numpy akshare apscheduler plotly streamlit loguru python-dotenv
uv add pytest pytest-asyncio httpx ruff
```

## 1.3 配置环境变量

创建 `.env`：

```env
APP_NAME=Fund Watcher
APP_ENV=dev

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=fund_watcher
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/fund_watcher

AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b
ONLINE_LLM_API_KEY=
```

## 1.4 Docker Compose 启动 PostgreSQL

```yaml
services:
  postgres:
    image: postgres:16
    container_name: fund_watcher_postgres
    environment:
      POSTGRES_DB: fund_watcher
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./docker/postgres/data:/var/lib/postgresql/data
```

---

# 第2阶段：数据库表设计

## 2.1 基金基础信息表：fund_info

用途：保存基金名称、类型、公司、经理、规模等静态或半静态信息。

字段建议：

```sql
CREATE TABLE fund_info (
    id BIGSERIAL PRIMARY KEY,
    fund_code VARCHAR(20) UNIQUE NOT NULL,
    fund_name VARCHAR(255) NOT NULL,
    fund_type VARCHAR(100),
    fund_company VARCHAR(255),
    fund_manager VARCHAR(255),
    establish_date DATE,
    fund_size NUMERIC(20, 4),
    risk_level VARCHAR(50),
    tracking_index VARCHAR(255),
    buy_status VARCHAR(50),
    sell_status VARCHAR(50),
    source VARCHAR(50),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

## 2.2 基金净值表：fund_nav

用途：保存每日单位净值、累计净值、日涨跌幅。

```sql
CREATE TABLE fund_nav (
    id BIGSERIAL PRIMARY KEY,
    fund_code VARCHAR(20) NOT NULL,
    nav_date DATE NOT NULL,
    unit_nav NUMERIC(20, 6),
    accumulated_nav NUMERIC(20, 6),
    daily_return NUMERIC(12, 6),
    source VARCHAR(50),
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(fund_code, nav_date)
);
```

## 2.3 用户自选基金表：watchlist

```sql
CREATE TABLE watchlist (
    id BIGSERIAL PRIMARY KEY,
    fund_code VARCHAR(20) NOT NULL,
    group_name VARCHAR(100) DEFAULT 'default',
    note TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(fund_code, group_name)
);
```

## 2.4 持仓表：portfolio_position

```sql
CREATE TABLE portfolio_position (
    id BIGSERIAL PRIMARY KEY,
    fund_code VARCHAR(20) NOT NULL,
    holding_amount NUMERIC(20, 4),
    holding_share NUMERIC(20, 4),
    cost_nav NUMERIC(20, 6),
    buy_date DATE,
    note TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

## 2.5 指标结果表：fund_indicator

```sql
CREATE TABLE fund_indicator (
    id BIGSERIAL PRIMARY KEY,
    fund_code VARCHAR(20) NOT NULL,
    calc_date DATE NOT NULL,
    return_1w NUMERIC(12, 6),
    return_1m NUMERIC(12, 6),
    return_3m NUMERIC(12, 6),
    return_6m NUMERIC(12, 6),
    return_1y NUMERIC(12, 6),
    max_drawdown_1y NUMERIC(12, 6),
    volatility_1y NUMERIC(12, 6),
    sharpe_1y NUMERIC(12, 6),
    win_rate_1y NUMERIC(12, 6),
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(fund_code, calc_date)
);
```

## 2.6 基金评分表：fund_score

```sql
CREATE TABLE fund_score (
    id BIGSERIAL PRIMARY KEY,
    fund_code VARCHAR(20) NOT NULL,
    score_date DATE NOT NULL,
    total_score NUMERIC(8, 2),
    return_score NUMERIC(8, 2),
    drawdown_score NUMERIC(8, 2),
    volatility_score NUMERIC(8, 2),
    stability_score NUMERIC(8, 2),
    size_score NUMERIC(8, 2),
    rating VARCHAR(50),
    reason TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(fund_code, score_date)
);
```

## 2.7 AI分析结果表：ai_report

```sql
CREATE TABLE ai_report (
    id BIGSERIAL PRIMARY KEY,
    report_type VARCHAR(50) NOT NULL,
    target_code VARCHAR(20),
    title VARCHAR(255),
    content TEXT NOT NULL,
    model_name VARCHAR(100),
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

# 第3阶段：数据采集模块

## 3.1 目标

先完成 3 类数据采集：

- 基金基础信息
- 基金历史净值
- 基金每日净值更新

## 3.2 文件位置

```text
app/data_source/
├── akshare_client.py
├── eastmoney_client.py
└── base.py
```

## 3.3 第一版接口设计

```python
class FundDataSource:
    def get_fund_info(self, fund_code: str) -> dict:
        ...

    def get_fund_nav_history(self, fund_code: str) -> pd.DataFrame:
        ...

    def get_fund_rank_list(self) -> pd.DataFrame:
        ...
```

## 3.4 采集任务

### 任务 1：根据基金代码拉取基金名称

输入：

```text
000001
```

输出：

```json
{
  "fund_code": "000001",
  "fund_name": "华夏成长混合",
  "fund_type": "混合型"
}
```

### 任务 2：拉取历史净值

输出字段标准化：

```text
fund_code
nav_date
unit_nav
accumulated_nav
daily_return
source
```

### 任务 3：批量更新自选基金净值

流程：

```text
读取 watchlist
→ 遍历基金代码
→ 拉取最新净值
→ fund_nav upsert
→ 记录日志
```

---

# 第4阶段：指标计算模块

## 4.1 文件位置

```text
app/services/indicator_service.py
```

## 4.2 需要计算的指标

### 收益率

```text
近1周收益率
近1月收益率
近3月收益率
近6月收益率
近1年收益率
```

公式：

```text
收益率 = 当前净值 / N日前净值 - 1
```

### 最大回撤

```text
最大回撤 = 当前净值 / 历史最高净值 - 1
```

### 波动率

使用日收益率标准差：

```text
年化波动率 = 日收益率标准差 * sqrt(252)
```

### 夏普比率

第一版可以简化：

```text
夏普比率 = 年化收益率 / 年化波动率
```

暂时不引入无风险利率。

### 胜率

```text
胜率 = 上涨天数 / 总交易天数
```

## 4.3 指标计算流程

```text
读取 fund_nav
→ 按 fund_code 分组
→ 用 Pandas 计算指标
→ 写入 fund_indicator
```

---

# 第5阶段：基金评分模型

## 5.1 文件位置

```text
app/services/score_service.py
```

## 5.2 第一版评分模型

总分 100 分：

| 维度 | 分值 |
|---|---:|
| 收益能力 | 30 |
| 回撤控制 | 25 |
| 波动控制 | 15 |
| 稳定性 | 15 |
| 基金规模 | 10 |
| 交易状态 | 5 |

## 5.3 评分规则示例

### 收益能力

```text
近1年收益 > 20%：30分
近1年收益 10%~20%：24分
近1年收益 5%~10%：18分
近1年收益 0%~5%：12分
近1年收益 < 0%：5分
```

### 回撤控制

```text
最大回撤 > -10%：25分
-10% ~ -20%：20分
-20% ~ -30%：12分
低于 -30%：5分
```

### 波动控制

```text
波动率 < 15%：15分
15%~25%：10分
25%~35%：6分
>35%：3分
```

### 评级输出

```text
85分以上：重点关注
70~85分：可以观察
60~70分：一般
60分以下：暂不关注
```

## 5.4 输出推荐原因

每只基金生成结构化原因：

```json
{
  "fund_code": "000001",
  "total_score": 82,
  "rating": "可以观察",
  "reasons": [
    "近1年收益表现较好",
    "最大回撤控制在可接受范围",
    "波动率偏高，需要注意风险"
  ]
}
```

---

# 第6阶段：FastAPI 接口开发

## 6.1 API 路由结构

```text
app/api/v1/
├── fund.py
├── watchlist.py
├── portfolio.py
├── indicator.py
├── score.py
└── report.py
```

## 6.2 基金接口

```text
GET /api/v1/funds/{fund_code}
GET /api/v1/funds/{fund_code}/nav
GET /api/v1/funds/{fund_code}/indicators
GET /api/v1/funds/{fund_code}/score
```

## 6.3 自选基金接口

```text
POST /api/v1/watchlist
GET /api/v1/watchlist
DELETE /api/v1/watchlist/{fund_code}
```

## 6.4 持仓接口

```text
POST /api/v1/portfolio
GET /api/v1/portfolio
PUT /api/v1/portfolio/{id}
DELETE /api/v1/portfolio/{id}
```

## 6.5 推荐接口

```text
GET /api/v1/recommendations/today
GET /api/v1/recommendations/top?limit=20
```

## 6.6 AI报告接口

```text
POST /api/v1/reports/daily
GET /api/v1/reports/latest
GET /api/v1/reports/fund/{fund_code}
```

---

# 第7阶段：定时任务

## 7.1 文件位置

```text
app/jobs/
├── scheduler.py
├── update_nav_job.py
├── calc_indicator_job.py
├── score_job.py
└── ai_report_job.py
```

## 7.2 每日任务流程

```text
15:30 更新基金估值/净值
18:00 拉取正式净值
18:10 计算指标
18:20 计算评分
18:30 生成AI日报
18:35 发送提醒
```

## 7.3 APScheduler 示例任务

```python
scheduler.add_job(update_fund_nav, "cron", hour=18, minute=0)
scheduler.add_job(calc_all_indicators, "cron", hour=18, minute=10)
scheduler.add_job(calc_all_scores, "cron", hour=18, minute=20)
scheduler.add_job(generate_daily_report, "cron", hour=18, minute=30)
```

---

# 第8阶段：Streamlit 看板

## 8.1 页面结构

```text
dashboard/
├── streamlit_app.py
├── pages/
│   ├── 1_我的持仓.py
│   ├── 2_自选基金.py
│   ├── 3_基金评分排行.py
│   ├── 4_基金详情.py
│   ├── 5_AI每日简报.py
│   └── 6_系统设置.py
```

## 8.2 首页展示

首页显示：

```text
今日组合涨跌
今日涨幅最高基金
今日跌幅最高基金
风险预警数量
推荐关注基金 Top 5
AI 每日简报摘要
```

## 8.3 基金详情页

展示：

```text
基金基本信息
净值曲线
近1月/3月/6月/1年收益
最大回撤
波动率
夏普比率
评分结果
AI解释
```

## 8.4 推荐排行页

展示字段：

```text
基金代码
基金名称
基金类型
近1年收益
最大回撤
波动率
总分
评级
推荐理由
```

---

# 第9阶段：预警系统

## 9.1 预警类型

```text
单日跌幅超过 3%
近1月回撤超过 8%
近3月排名明显下降
基金规模过小
基金经理变更
持仓基金相关性过高
同主题基金过多
评分下降超过 10 分
```

## 9.2 预警表：alert_event

```sql
CREATE TABLE alert_event (
    id BIGSERIAL PRIMARY KEY,
    alert_type VARCHAR(100) NOT NULL,
    fund_code VARCHAR(20),
    alert_level VARCHAR(20),
    title VARCHAR(255),
    content TEXT,
    is_read BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW()
);
```

## 9.3 推送方式

第一版：

```text
Streamlit页面提醒
```

第二版：

```text
邮件提醒
```

第三版：

```text
企业微信 / Server酱 / Telegram Bot
```

---

# 第10阶段：AI 分析助手

## 10.1 AI 不做什么

AI 不直接输出：

```text
立即买入
立即卖出
重仓买入
保证收益
```

## 10.2 AI 做什么

AI 负责：

```text
解释基金变化
总结风险点
生成日报
总结基金季报
对比两只基金
解释评分原因
```

## 10.3 AI 模块结构

```text
app/services/ai/
├── base.py
├── ollama_client.py
├── online_client.py
├── prompt_templates.py
└── report_service.py
```

## 10.4 Prompt 模板：基金日报

```text
你是一个基金投研助手。请根据以下结构化数据，生成一份简洁、谨慎、可解释的基金日报。

要求：
1. 不要承诺收益
2. 不要直接给出买卖指令
3. 重点解释风险和变化原因
4. 输出分为：今日概况、重点关注、风险提醒、明日观察

数据：
{fund_data}
```

## 10.5 在线模型和本地模型分工

| 任务 | 推荐模型 |
|---|---|
| 简单解释 | 本地 Ollama |
| 日报生成 | 在线模型 |
| 季报长文本总结 | 在线模型 |
| 本地隐私问答 | 本地 Ollama |
| 批量低成本处理 | 本地 Ollama |

---

# 第11阶段：组合分析

## 11.1 持仓收益计算

```text
当前市值 = 持有份额 * 最新单位净值
收益金额 = 当前市值 - 投入金额
收益率 = 收益金额 / 投入金额
```

## 11.2 基金相关性分析

基于日收益率计算相关系数：

```text
corr(fund_a_daily_return, fund_b_daily_return)
```

用于发现：

```text
两只基金走势高度相似
同类基金重复配置
组合分散度不足
```

## 11.3 组合风险提示

```text
单只基金占比超过 30%
权益类基金占比过高
同主题基金数量过多
高相关基金超过 3 只
近1月组合回撤超过阈值
```

---

# 第12阶段：Docker 本地部署

## 12.1 docker-compose 最终服务

```yaml
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: fund_watcher
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  backend:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    depends_on:
      - postgres
    ports:
      - "8000:8000"
    env_file:
      - .env

  dashboard:
    build: .
    command: streamlit run dashboard/streamlit_app.py --server.port 8501 --server.address 0.0.0.0
    depends_on:
      - backend
    ports:
      - "8501:8501"
    env_file:
      - .env
```

---

# 第13阶段：测试计划

## 13.1 单元测试

```text
test_indicator_service.py
test_score_service.py
test_data_source.py
test_portfolio_service.py
```

## 13.2 API 测试

```text
test_fund_api.py
test_watchlist_api.py
test_portfolio_api.py
test_recommendation_api.py
```

## 13.3 重点测试用例

```text
基金代码不存在
净值数据为空
净值日期断档
重复添加自选基金
评分指标缺失
数据库连接失败
AI接口失败后降级
```

---

# 第14阶段：日志与异常处理

## 14.1 日志内容

```text
采集开始/结束
采集基金数量
失败基金代码
入库数量
指标计算耗时
评分结果数量
AI生成耗时
异常堆栈
```

## 14.2 异常处理

```text
数据源请求失败：重试3次
单只基金失败：跳过，不影响整体任务
AI失败：使用规则解释兜底
数据库失败：记录日志并终止任务
```

---

# 第15阶段：推荐开发顺序

## 第1周：跑通最小闭环

目标：

```text
基金代码 → 拉取净值 → 入库 → 计算指标 → 页面展示
```

任务：

- [ ] 初始化项目
- [ ] 启动 PostgreSQL
- [ ] 创建 fund_info / fund_nav / watchlist 表
- [ ] 接入 AKShare
- [ ] 拉取单只基金历史净值
- [ ] 写入 PostgreSQL
- [ ] 计算近1月/3月/6月/1年收益
- [ ] Streamlit 展示净值曲线

## 第2周：加入评分和推荐

目标：

```text
自选基金 → 指标计算 → 评分排行 → 推荐理由
```

任务：

- [ ] 增加 fund_indicator 表
- [ ] 增加 fund_score 表
- [ ] 实现最大回撤
- [ ] 实现波动率
- [ ] 实现夏普比率
- [ ] 实现评分模型
- [ ] 推荐 Top 10 页面
- [ ] 基金详情页展示评分原因

## 第3周：加入持仓和风险预警

目标：

```text
我的持仓 → 盈亏统计 → 风险提醒
```

任务：

- [ ] 增加 portfolio_position 表
- [ ] 增加 alert_event 表
- [ ] 实现持仓收益计算
- [ ] 实现单日大跌预警
- [ ] 实现最大回撤预警
- [ ] 实现评分下降预警
- [ ] 页面展示风险提醒

## 第4周：加入 AI 简报

目标：

```text
结构化数据 → AI解释 → 每日基金简报
```

任务：

- [ ] 接入 Ollama
- [ ] 设计 AI Prompt 模板
- [ ] 生成单只基金解释
- [ ] 生成每日简报
- [ ] AI失败时使用规则解释兜底
- [ ] 保存 ai_report
- [ ] 页面展示 AI 简报

## 第5周：完善部署和作品集

目标：

```text
Docker 一键启动 + README + 演示截图
```

任务：

- [ ] 完善 Dockerfile
- [ ] 完善 docker-compose
- [ ] 添加 .env.example
- [ ] 添加 README
- [ ] 添加系统架构图
- [ ] 添加页面截图
- [ ] 添加接口文档
- [ ] 添加测试用例
- [ ] 准备面试讲解稿

---

# 第16阶段：MVP 验收标准

完成以下内容，就算第一版成功：

- [ ] 可以添加基金到自选列表
- [ ] 可以拉取基金历史净值
- [ ] PostgreSQL 成功保存数据
- [ ] 可以计算收益率、回撤、波动率
- [ ] 可以给基金打分
- [ ] 可以展示基金净值曲线
- [ ] 可以看到推荐排行
- [ ] 可以生成 AI 解释
- [ ] 可以 Docker 本地启动

---

# 第17阶段：后续增强方向

## 17.1 数据增强

- 基金经理历史业绩
- 基金规模变化
- 基金持仓行业
- 基金季报公告
- 同类基金排名
- 指数走势对比

## 17.2 策略增强

- 同类基金横向排名
- 风格漂移检测
- 持仓重合度分析
- 定投区间识别
- 估值百分位辅助判断

## 17.3 AI增强

- RAG读取基金公告
- 自动总结季报
- 自动解释重仓股变化
- 生成周报/月报
- 语音播报简报

## 17.4 工程增强

- Alembic 数据库迁移
- Celery + Redis 任务队列
- Vue 前端
- 用户登录
- 多组合管理
- Prometheus 监控
- GitHub Actions

---

# 第18阶段：面试/作品集讲解重点

你可以这样介绍这个项目：

```text
这是一个本地部署的基金投研助手，核心目标是通过自动化数据采集、指标计算、规则评分和 AI 总结，帮助个人投资者持续监控基金组合风险和候选基金变化。

我使用 FastAPI 作为后端服务，PostgreSQL 保存基金净值、指标和评分结果，APScheduler 执行每日定时更新任务，Pandas 计算收益率、最大回撤、波动率和夏普比率，Streamlit 构建本地看板。

AI 部分没有直接做买卖预测，而是基于结构化指标和规则评分结果生成可解释的基金分析报告，从而降低大模型幻觉对投资决策的影响。
```

可突出能力：

```text
Python后端工程能力
PostgreSQL数据建模
定时任务调度
Pandas金融指标计算
数据可视化
AI应用落地
Docker本地部署
异常处理和降级设计
```

---

# 第19阶段：最小可执行任务清单

如果你今天就开始，按这个顺序做：

1. 创建项目目录
2. 用 Docker 启动 PostgreSQL
3. 写 SQLAlchemy 数据库连接
4. 建 `fund_info`、`fund_nav`、`watchlist` 三张表
5. 写 AKShare 单只基金历史净值采集函数
6. 把净值数据写入 PostgreSQL
7. 写收益率计算函数
8. 写 Streamlit 页面画净值曲线
9. 加入自选基金增删查
10. 做评分函数
11. 做推荐排行页
12. 接入 Ollama 生成解释

---

# 第20阶段：注意事项

## 20.1 投资风险

本系统只做数据监控和辅助分析，不保证收益，不应直接作为买卖依据。

## 20.2 数据可靠性

免费数据源可能存在：

- 延迟
- 字段变化
- 接口不可用
- 数据缺失

所以需要：

- 多数据源兜底
- 日志记录
- 失败重试
- 数据校验

## 20.3 AI 风险

AI 可能出现：

- 错误归因
- 过度自信
- 忽略风险
- 生成不准确结论

所以需要：

- 只基于结构化数据生成解释
- Prompt 中明确禁止承诺收益
- 保留原始指标
- AI失败时使用规则解释兜底

---

## 结论

建议第一版聚焦：

```text
PostgreSQL + FastAPI + AKShare + Pandas + Streamlit + Ollama
```

先做出一个真正能每天看的基金监控系统，再逐步加在线模型、RAG、季报总结和组合风险分析。
