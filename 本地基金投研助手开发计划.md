# FundPilot 当前开发计划

## 项目目标

FundPilot 要做成一个真正可用的本地基金助手，而不是演示型功能集合。每个功能都要对应明确使用场景，并持续提升日常使用体验。

## 当前技术栈

| 层级 | 技术 |
| --- | --- |
| 前端工作台 | Vue 3、TypeScript、Vite、Element Plus、ECharts |
| 后端 API | FastAPI、Pydantic |
| 数据库 | PostgreSQL、SQLAlchemy、Alembic |
| 数据处理 | Pandas、NumPy |
| 数据源 | AKShare、Eastmoney |
| 任务调度 | APScheduler，可选开启 |
| AI 简报 | Ollama + 规则兜底 |
| 测试 | pytest、ruff |

## 核心数据链路

```text
watchlist -> sync_nav -> fund_nav -> indicators -> scores -> Vue/report
market indexes -> market_index_daily -> market context -> Vue/report
portfolio_position + latest nav -> portfolio overview -> alerts/report
fund_nav daily_return -> correlation matrix -> high-correlation alerts
task execution -> task_run_log -> task center
```

## 功能场景

1. 自选基金管理：用户维护观察池，后续同步和分析都围绕 active 自选基金展开。
2. 数据质量：用户发现净值过旧、断档、缺失涨跌幅和待计算指标，避免基于不完整数据做判断。
3. 基金详情：用户对单只基金做同步、指标计算、评分和净值/回撤/涨跌幅观察。
4. 基金对比：用户横向比较 2-5 只基金的收益、风险、波动、评分和行业主题。
5. 评分排行：用户快速筛选规则评分较高、理由清晰的候选基金。
6. 评分趋势：用户观察单只基金评分变化，辅助复盘规则指标变化。
7. 我的持仓：用户录入买入记录，跟踪组合市值、收益率和持仓集中度。
8. 相关性分析：用户识别走势过于相似的基金，减少重复配置。
9. 风险预警：系统提示大跌、回撤、评分下降、集中度和高相关风险。
10. AI 简报：系统基于结构化数据生成谨慎总结，失败时使用规则兜底。
11. 任务中心：用户按数据链路手动运行任务，并查看最近执行日志。

## 近期优化方向

- Vue 前端：补齐空状态、加载状态、错误提示、字段中文化和图表可读性。
- API 契约：为 Vue 页面新增必要聚合接口，避免前端复制业务逻辑。
- 数据链路：让“添加自选 -> 一键同步并分析 -> 评分/预警/报告”成为最顺的主流程。
- 文档：README、架构、数据库、评分模型、AI 安全和前端说明保持一致。
- 测试：后端服务和 API 契约用 pytest 固化，前端在 Node/npm 可用后补 typecheck/build 和浏览器验收。

## 产品边界

FundPilot 只做数据监控、规则评分和解释性总结，不承诺收益，不提供直接买入、卖出或重仓建议。AI 只基于本地结构化数据生成总结，不自由联网搜索。
