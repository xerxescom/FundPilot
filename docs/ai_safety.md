# FundPilot AI 安全边界

FundPilot 的 AI 简报只负责解释结构化数据，不直接参与评分计算，也不输出买卖指令。

## 输入范围

- 自选基金数量和评分。
- 市场指数表现。
- 数据健康状态。
- 持仓概况。
- 风险预警。
- 高相关基金组合。

## 输出限制

报告不能包含直接买入、卖出、重仓、保证收益等表达。系统会在输出后做关键词过滤；如果发现受限表达，会改用规则兜底报告。

## 兜底机制

当 Ollama 不可用、超时、返回空内容或输出触发安全过滤时，系统会保存 `rule-fallback` 报告，并记录失败原因和输入数据摘要。
